import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    background: {
      default: "#FFF6E9", // Set your desired background color here
    },
  },
});

export default theme;
